# Backend listening port.
PORT=5050
# Allowed frontend origin for credentialed CORS requests.
FRONTEND_ORIGIN=http://localhost:5173
# Bootstrap admin account (development/default seed).
ADMIN_EMAIL=admin@dlife.local
# REQUIRED: set a strong password before deployment.
ADMIN_PASSWORD=change-me-now
